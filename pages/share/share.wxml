<view class='s-container'>
  <block wx:for-items="{{ shares }}" wx:key="{{ index }}">
    <view class='s-item' bindtap='openShareDtl'>
      <image src='{{item.url}}' mode="aspectFill"></image>
      <text>{{item.title}}</text>
    </view>
  </block>

</view>